# Stonk Console

Standalone frontend for the Stonk backend.

## Run

```bash
node server.js
```

Defaults:
- Frontend: `http://localhost:4173`
- API target: `http://localhost:8080`

Override the API target if needed:

```bash
$env:API_TARGET="http://localhost:8080"
node server.js
```
