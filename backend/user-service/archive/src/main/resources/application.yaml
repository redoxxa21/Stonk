server:
  port: 2625

spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/portfolio_db
    username: stonks
    password: stonks123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  properties:
    hibernate:
      format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka